"%test":
  quarkus:
    http:
      port: 7777

  security:
    keycloak:
      cert:
        url: http://keycloak:8080/realms/hormigas/protocol/openid-connect/certs
      request:
        rate-limit: 1000

  processing:
    messages:
      persistence:
        batch-size: 10
        timeout-min: 1
        sending-queue-size: 20
        ack-queue-size: 20
      scheduler:
        time-interval-sec: 1